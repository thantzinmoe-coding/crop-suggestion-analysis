# Database migrations

Alembic migration files live in `versions`. Import new SQLAlchemy models in `app.db.base` or a central model registry before running autogeneration so their metadata is visible.

